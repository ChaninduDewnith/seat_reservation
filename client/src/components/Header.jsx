function Header() {
  return (
    <header>
      <nav className="container">
        <a href="#" className="logo">Seat Reservation</a>
        <a href="/login" className="login-btn">Login</a>
      </nav>
    </header>
  );
}

export default Header;
